# Core dependencies — one pip install, no model downloads needed.
streamlit>=1.35.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
pypdf>=4.0.0
docx2txt>=0.8

# Optional upgrades (uncomment to enable):
# anthropic>=0.40.0              # AI deep review with Claude (needs an API key)
# sentence-transformers>=2.6.0   # meaning-aware semantic matching (downloads a ~90MB model)
# pdfplumber>=0.11.0             # fallback extractor for stubborn PDF layouts
